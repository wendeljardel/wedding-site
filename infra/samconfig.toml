version = 0.1

[default.deploy.parameters]
stack_name = "wedding-site"
region = "sa-east-1"
confirm_changeset = true
capabilities = "CAPABILITY_IAM"
resolve_s3 = true

# Lembre de informar o parametro AdminToken na primeira execucao,
# por ex.: sam deploy --parameter-overrides AdminToken=$(openssl rand -hex 32)
